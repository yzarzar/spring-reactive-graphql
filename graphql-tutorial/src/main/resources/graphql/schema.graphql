type Query {
    getPlayerById(id: ID): Player
}

type Mutation {
    addPlayer(playerInput: PlayerInput!): Player
}

type Player {
    id: ID
    name: String
}

input PlayerInput {
    name: String!
}